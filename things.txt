            {/* Group 4: Call Preferences */}
            <motion.div
              className={cn(
                "relative p-6 -m-6 rounded-2xl transition-colors duration-300",
                focusedGroup === 4 && "bg-card/50"
              )}
              onFocus={() => setFocusedGroup(4)}
            >
              <FormGroupHeader title={t.form.callPreferences || "Preferred Call Time"} step={4} />

              <div className="space-y-6">
                {/* Preferred Day */}
                <div>
                  <label htmlFor="preferredDay" className="block text-sm font-medium text-foreground mb-2">
                    {t.form.preferredDay} *
                  </label>
                  <select
                    id="preferredDay"
                    value={formData.preferredDay}
                    onChange={(e) => setFormData({ ...formData, preferredDay: e.target.value })}
                    className={cn(
                      "w-full px-4 py-3 bg-card border rounded-lg text-foreground",
                      "focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary transition-all duration-300 appearance-none",
                      errors.preferredDay ? "border-destructive" : "border-border/50",
                      !formData.preferredDay && "text-muted-foreground/50"
                    )}
                  >
                    <option value="">{t.form.projectGoalPlaceholder}</option>
                    {t.form.dayOptions.map((option) => (
                      <option key={option} value={option}>
                        {option}
                      </option>
                    ))}
                  </select>
                  <AnimatePresence>
                    {errors.preferredDay && <ValidationMessage message={errors.preferredDay} />}
                  </AnimatePresence>
                </div>

                {/* Preferred Time */}
                <div>
                  <label htmlFor="preferredTime" className="block text-sm font-medium text-foreground mb-2">
                    {t.form.preferredTime} *
                  </label>
                  <select
                    id="preferredTime"
                    value={formData.preferredTime}
                    onChange={(e) => setFormData({ ...formData, preferredTime: e.target.value })}
                    className={cn(
                      "w-full px-4 py-3 bg-card border rounded-lg text-foreground",
                      "focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary transition-all duration-300 appearance-none",
                      errors.preferredTime ? "border-destructive" : "border-border/50",
                      !formData.preferredTime && "text-muted-foreground/50"
                    )}
                  >
                    <option value="">{t.form.projectGoalPlaceholder}</option>
                    {t.form.timeOptions.map((option) => (
                      <option key={option} value={option}>
                        {option}
                      </option>
                    ))}
                  </select>
                  <AnimatePresence>
                    {errors.preferredTime && <ValidationMessage message={errors.preferredTime} />}
                  </AnimatePresence>
                </div>

                {/* Additional Notes */}
                <div>
                  <label htmlFor="additionalNotes" className="block text-sm font-medium text-foreground mb-2">
                    {t.form.additionalNotes}
                  </label>
                  <textarea
                    id="additionalNotes"
                    value={formData.additionalNotes}
                    onChange={(e) => setFormData({ ...formData, additionalNotes: e.target.value })}
                    rows={4}
                    placeholder={t.form.additionalNotesPlaceholder}
                    className="w-full px-4 py-3 bg-card border border-border/50 rounded-lg text-foreground placeholder:text-muted-foreground/50 focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary transition-all duration-300 resize-none"
                  />
                </div>
              </div>
            </motion.div>